# packages/api-types

Placeholder, per `docs/adr/0007-repo-and-hosting-shape.md` and
`docs/stage2-monorepo-plan.md` section 1. Will hold shared TS types generated
from the companion API's OpenAPI spec and the new lineup JSON read API, once
the "Formalize API contracts" workstream (`docs/roadmap.md` section 3.2)
lands. Empty at Stage 2 exit.
