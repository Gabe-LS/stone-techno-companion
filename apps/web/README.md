# apps/web

Empty placeholder at Stage 2 exit, per `docs/adr/0007-repo-and-hosting-shape.md`
(monorepo layout: `apps/web`, `services/companion`, `services/data`, `packages/`).

The Next.js app itself is Stage 3 scope (see `docs/roadmap.md` section 3.3, and
`docs/stage2-monorepo-plan.md` section 5, non-goals). Nothing here renders yet.
