This confirms the contrast: `chat_ws.py:1672,1686` explicitly SELECTs and reads `media_url` with a fallback to `content.get("url")`. The two `chat_db.py` functions never do this — they SELECT only `content` and never touch `media_url`. This directly contradicts the CLAUDE.md invariant claim. Reachability of the precondition (E2EE image/video DM) is well-established by the architecture docs — E2EE DMs store the plaintext URL only in `media_url`, per CLAUDE.md's own E2EE section.

Verdict: **CONFIRMED**

Reasoning:
- `server/chat_db.py:889` (`purge_expired_messages`) — query is `SELECT id, room_id, type, content FROM messages WHERE expires_at <= ?`. No `media_url` in the column list. File cleanup at lines 897-905 only runs `content = json.loads(msg["content"]); if "url" in content: image_paths.append(content["url"])`. For an E2EE envelope (`{e2ee:true, v:2, sd:..., ct:..., keys:{...}}`), there is no `"url"` key, so `image_paths` never gets the file and it's never unlinked.
- `server/chat_db.py:1259` (`delete_user_messages`) — same pattern: `SELECT id, room_id, type, content FROM messages WHERE user_id = ? AND expires_at > ?`, then `content.get("url", "")` at line 1272. Same gap — `media_url` is never selected or read.
- Contrast confirms the gap is real and known-fixable elsewhere: `server/chat_ws.py:1672` (`delete_message` WS handler) explicitly selects `media_url` alongside `content`, and line 1685-1691 prefers `msg_row["media_url"]` when present, falling back to `content.get("url")` only for legacy/non-E2EE rows. This is the one path that actually implements the invariant CLAUDE.md describes.
- This directly contradicts the CLAUDE.md documentation, which states both TTL purge and manual delete read `media_url` — that's only true for the interactive WS delete-message path, not for `purge_expired_messages` (TTL purge) or `delete_user_messages` (ban/mute mass-delete).
- Precondition reachability: E2EE is DM-only and automatic for any DM with both devices keyed (default path per CLAUDE.md's E2EE section), so any image/video sent in an E2EE DM that later expires via TTL, or whose sender is subsequently muted/banned, hits this gap. This is a normal, easily reachable flow, not an edge case.

Net effect: confirmed unbounded disk growth for E2EE DM media on TTL expiry and on admin mute/ban mass-delete — the claim is accurate as stated, including its correction of the CLAUDE.md invariant.
