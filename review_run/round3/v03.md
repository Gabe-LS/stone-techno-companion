Verdict: CONFIRMED

Reasoning: The code matches the claim exactly.

- `server/chat/chat.html:2479` — `let outgoingContent = content;` (plaintext by default, before any encryption attempt).
- `server/chat/chat.html:4314-4320` (`_fetchDeviceList`) — only a genuine `404` returns `null`; any other non-ok response throws `Error('device list fetch failed: HTTP ' + res.status)`, and network-level failures (timeout, connection drop) throw naturally from `fetch`. `getDeviceList` (4335-4347) propagates this rethrow unchanged. `encrypt()` (4364+) has no try/catch of its own around `getDeviceList`, so the exception passes straight out to the caller.
- `server/chat/chat.html:2493-2522` (`sendChatMessage`) — the `catch (e)` block at 2512 does not inspect `e` at all (no check for a 404-derived "no key" signal vs. a generic thrown error). It branches purely on `_roomEncrypted.has(roomId)`: if true, fails closed (blocks send); if false, silently adds the room to `_unencryptedRooms`, shows a dismissible toast ("Encryption unavailable for this user"), and falls through with `outgoingContent` still equal to plaintext `content`.
- `server/chat/chat.html:2545,2553` — `payload.content = outgoingContent` is sent via `wsSend('send_message', payload)` unconditionally after the try/catch, with no further gate.
- `_roomEncrypted` (declared as `new Set()` at line 405) is populated **only** by successful local encrypt/decrypt events this session: `_decryptV2`/`_decryptV1Legacy` add to it on successful decrypt (lines ~1166-1210), and `encrypt()`'s success branch adds to it (line 2508). It is never seeded from server state.
- Critically, the server-truthful signal `other_has_key` (from `GET /chat/api/dms`, line 1867) is wired only into `_unencryptedRooms` (for the lock-icon UI), never into `_roomEncrypted`. So even when the server has already told the client "this peer has a registered device key" (i.e., this is a real encrypted conversation), that fact does not protect the send path — the fail-closed gate only trusts *this session's own* prior successful crypto operations.

This produces exactly the described gap: a DM opened fresh this session (plausible given the 24h TTL and in-memory reset on reload) where the user sends before ever decrypting an incoming message, combined with one transient `GET /chat/api/keys/{peer}` failure (500, timeout, dropped connection), causes a real E2EE-capable conversation to send that single message in plaintext to the server — silently, past a dismissible toast, with no retry and no fail-closed protection.

I also checked CLAUDE.md's documented E2EE fallback: "keyless peers (no registered devices) fall back to plaintext" — this is explicitly scoped to the *no-devices* (404/null) case only. Nothing documents transient-error-triggered plaintext fallback as an intentional trade-off, so this isn't a known/accepted design decision — it's an unguarded conflation of two distinct failure modes in the catch block.
