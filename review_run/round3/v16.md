## Verification Results

**Claim A: HIGH — CONFIRMED**

Verdict: CONFIRMED

Reasoning: `deploy.sh:109-142` (repo root, not `server/`). The health check (lines 116-124) does two `docstring inspect ... State.Health.Status` probes but the `if [ "$STATUS" = "healthy" ]` gate (line 117) only wraps the *first* check — the retry after `sleep 30` (line 122-123) just echoes the result with no conditional at all. The chat API probe (lines 126-131) only ever prints `"WARNING: Chat API not responding!"` on failure (line 130) — no `exit 1` anywhere in this block. After that, lines 134-142 unconditionally prune old backups and print `=== Deploy complete ===`, falling off the end of the script (implicit exit 0). `set -euo pipefail` doesn't help here since every health-check command has an `||` fallback or lives inside an `if`, so nothing propagates a non-zero status. There is no code anywhere in the file that restores a previous image, git commit, or the timestamped backup created in earlier steps — the backups just sit on disk unused. A crash-looping container after deploy produces console warnings only and reports success.

**Claim B: HIGH — CONFIRMED**

Verdict: CONFIRMED

Reasoning: `scraper/scrape.py:186-196` — the assignment loop selects on `li.tab-list__list-item.lineup-name[data-timestamp]` (requires only `data-timestamp`, not `data-overlay-identifier`), then reads `overlay_id = (li.get("data-overlay-identifier") or "").strip()` with no `if not overlay_id: continue` guard (contrast with the overlay-detail loop's guard at line 145-146). Any TBA/placeholder lineup slot lacking that attribute, or bearing an id with no matching overlay-detail panel, yields an `overlay_id` absent from `parsed["artists"]`.

`scraper/db.py:20-99` confirms `PRAGMA foreign_keys=ON` (line 22) and `schedule.artist_id TEXT NOT NULL REFERENCES artists(id)` (line 90) — an enforced, non-nullable FK. `upsert_lineup` (db.py:192-230) only creates `artists` rows from `parsed["artists"].items()` (line 192), then does `INSERT OR IGNORE INTO schedule ...` (line 218) using `assignment["overlay_id"]` — if that id was never inserted into `artists`, the FK target doesn't exist.

I confirmed via SQLite's own documentation (fetched `lang_conflict.html`) that this is NOT suppressed: *"the IGNORE conflict resolution algorithm works like ABORT for foreign key constraint errors"* — unlike UNIQUE/NOT NULL/CHECK violations, which IGNORE does silently skip. So the `INSERT OR IGNORE` raises `sqlite3.IntegrityError` exactly as an unqualified INSERT would.

Tracing the call path (`stone_techno_companion.py:141-156`): `upsert_lineup(db, parsed, event_id)` is called with no surrounding `try/except` — only an outer `try/finally: db.close()` (lines 131-224) that has no exception handler. So the `IntegrityError` propagates uncaught out of `main()`, crashing the entire full-pipeline run (scrape + enrich + photos + render), consistent with the claim. This only affects the full scrape path — `--render-only` skips `scrape_lineup`/`upsert_lineup` entirely and is unaffected — but the documented default (`python stone_techno_companion.py`) hits it.

Note: I could not empirically execute a confirming Python/sqlite3 script — my Bash tool (and a subagent's) both failed with a persistent `EPERM: operation not permitted, mkdir '.../session-env/...'` that appears to be a harness-level session issue, not the sandbox toggle (both sandboxed and unsandboxed attempts failed identically). I substituted direct reading of the official SQLite documentation for the FK/IGNORE semantics claim, which is unambiguous and directly on point. You may want to restart or recreate your Claude Code session — this Bash breakage will block any actual code execution/testing until resolved.
