All line numbers cited in the claims match exactly. Both claims are fully confirmed.

## Claim A: CONFIRMED

**Reasoning:**
- `create_message` (chat_db.py:763-807) executes a synchronous `INSERT` + `db.commit()` (chat_db.py:794-796), returning immediately — no moderation has run yet.
- `get_room_messages` (chat_db.py:810-824) filters only on `WHERE m.room_id = ? AND m.expires_at > ?` — there is no `moderation_status`/`pending`/`approved` column on the `messages` table (verified against the `CREATE TABLE messages` schema), so a message sits in the "allowed" and "rejected-but-not-yet-deleted" states indistinguishably.
- `join_room` (chat_ws.py:1148-1164) calls `get_room_messages` directly and ships every row as `room_history`, unconditionally formatted by `_format_message_for_history` (chat_ws.py:731-767), which also has no moderation gating.
- The live broadcast is correctly gated: `mgr.broadcast_to_room(room_id, event_data, ...)` only executes after the `if not mod_result["allowed"]: ... return` early-return block (chat_ws.py:797-860), i.e., only on the success path (chat_ws.py:862+).
- On rejection, `message_removed` is sent via `mgr.send_to_user(user_id, ...)` (chat_ws.py:814-822) — sender only, never `broadcast_to_room`. Anyone who called `join_room` between the synchronous commit (chat_ws.py:1373-1382) and the eventual `DELETE FROM messages` (chat_ws.py:812) sees the not-yet-moderated content via `room_history`, and receives no retraction event since `message_removed`/`messages_expired` never reach non-sender room members for a single rejected message (the `messages_expired` broadcast at 823-833 only fires for ban/mute bulk-delete, not the single-message reject path).
- `join_room` is a normal, frequently-triggered client action (room switch, reconnect, tab refocus) — the precondition (racing the moderation window, typically hundreds of ms to a couple seconds for the OpenAI calls) is easily reachable, not a contrived edge case.

## Claim B: CONFIRMED

**Reasoning:**
- The moderation task is created as a bare `asyncio.create_task(_moderate_and_broadcast(...))` (chat_ws.py:1423-1442) with no reference retained anywhere — confirmed untracked/unawaited.
- `api.py`'s lifespan shutdown (`_check_vapid_key_consistency`, then on shutdown: cancels/awaits `task`, `prune_task`, `push_task`, `chat_purge_task` only) has no knowledge of per-message moderation tasks — they are not in that tracked set, so app-level graceful shutdown does nothing to protect them.
- `deploy.sh:107` runs `docker compose up -d --build --force-recreate`, which recreates the `stone-techno` container — Docker sends SIGTERM to the old container, and `server/docker-compose.yml` has no `stop_grace_period` override, so Docker's default 10s SIGTERM→SIGKILL window applies.
- If `_moderate_and_broadcast` is killed mid-flight (whether via early event-loop teardown during uvicorn's own graceful shutdown, or the hard SIGKILL after the 10s grace period), the coroutine never reaches its `DELETE FROM messages` (chat_ws.py:812) or its `finally` cleanup. The message row — already committed synchronously and pre-moderation (per Claim A) — is left permanently in the DB with no marker distinguishing it from an approved message, and remains servable via `get_room_messages`/`room_history` indefinitely (bounded only by the room's TTL, up to 24h per `chat_settings`).
- This is a real, reproducible bypass window on every force-recreate deploy performed while messages are in flight — not a hypothetical.

Both findings hold at HIGH severity as stated. Root cause in both cases is the same design gap: messages are persisted *before* moderation completes, and there's no status column to distinguish "committed but unmoderated" from "approved" — `room_history` and the DB itself are blind to that intermediate state.
