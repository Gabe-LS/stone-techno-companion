## Verification Report

**Claim: A**
**Verdict: CONFIRMED**
**Reasoning:** `WordFilter.check()` at server/chat_moderation.py:116-132 normalizes text (`_normalize`, lines 81-88 — lowercase, diacritics stripped, leetspeak substitutions, non-alnum stripped) then splits strictly on whitespace (`words = normalized.split()`, line 118) and only matches when a contiguous slice of whitespace-delimited tokens equals the blocklist term's tokens exactly (`words[i:i+len(term_words)] == term_words`, line 123/129). There is no substring/`in` check anywhere in `check()`. By contrast, `check_username()` (lines 134-148) explicitly adds a substring fallback after calling `check()` on each dot/underscore/hyphen-split part: `if len(term) >= min_len and term in part and part != term` (line 146), with a comment acknowledging this is needed "to catch e.g. 'ket' in 'ketlover'". `moderate_message()` (line 436) calls `wf.check(text)` directly on the raw message — the substring-aware path is never invoked for message content. Gluing a term into a longer single token (`"buymdmanow"`, `"areyouanigger"`) produces one whitespace-token that will never equal a blocklist term-tuple, so the instant word-filter gate is bypassed by simply removing spaces around the flagged word. (It would still be subject to the two AI layers downstream, but the instant/free gate is defeated as claimed.)

**Claim: B**
**Verdict: CONFIRMED**
**Reasoning:** `moderate_message()` runs both AI checks concurrently via `asyncio.gather(..., return_exceptions=True)` (lines 450-456), then independently sets `ai_errored`/`drug_errored` booleans (lines 460-461) and nulls out whichever result errored (lines 462-467). The only fail-closed branch is line 469: `if ai_errored and drug_errored and os.environ.get("OPENAI_API_KEY"): return {"allowed": False, ...}` — this requires **both** tasks to have raised. If only `check_openai_moderation` (sexual/violence/hate/self-harm/harassment, per `OPENAI_THRESHOLDS` lines 157-170) errors while `check_content_detection` (drugs/spam/payment/external-links, per the disjoint category list in `CONTENT_DETECTION_PROMPT` lines 174-197) succeeds and returns `None` (not flagged, since the content is outside the drug layer's scope, e.g. graphic violence or hate speech), then: `ai_errored=True, drug_errored=False` → the line 469 condition is `False` → execution falls through; `ai_result` is `None` (set at line 464) so the `if ai_result:` block (476) is skipped; `drug_result` is `None`/falsy so the `if drug_result:` block (504) is skipped; execution reaches line 519 `return {"allowed": True}`. The message is allowed despite one full moderation layer (and its exclusive category coverage) having failed outright. This is a genuine fail-open gap for single-layer outages, exactly as claimed.
