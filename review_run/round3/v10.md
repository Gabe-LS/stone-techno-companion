Verified all three claims by reading `server/chat_ws.py` in full (lines 1–1894) and cross-checking `server/chat_db.py` and `server/chat_api.py` via read-only sub-agents.

---

**Claim: A**
**Verdict: CONFIRMED**
**Reasoning:** `ConnectionManager.check_rate_limit` (chat_ws.py:681-690, 10 msgs/10s per user) is called only at line 1334 (`send_message`) and line 1460 (`create_meetup`). The `typing` handler (1444-1457) calls `manager.broadcast_to_room` directly with no rate check. `add_reaction` (1601-1633) and `remove_reaction` (1634-1666) do the same. `join_room`/`leave_room` presence fan-out (`ConnectionManager.join_room` 580-621, `leave_room` 623-643) also broadcast unconditionally — join only skips the broadcast if the user is *already* in the room (line 607 `already_in_room` check), so a client can still trigger a broadcast on every alternating join/leave pair. `ChatRoom.broadcast` (502-517) is O(room member count), awaiting `ws.send_text` per connection. A follow-up Explore-agent search confirmed there is no IP-level or connection-level throttle anywhere else in the stack (`api.py` has per-IP limits for lineup endpoints and a `MAX_WS_PER_SESSION=20` cap, but nothing bounds per-connection WS *message* frequency for chat besides the per-event `check_rate_limit` calls already noted). The precondition (an authenticated WS connection in a populous room, e.g. the main/auto-join room) is trivially reachable. Confirmed as described.

**Claim: B**
**Verdict: CONFIRMED**
**Reasoning:** `report_message` (chat_ws.py:1714-1770) never calls `check_rate_limit` — confirmed by direct inspection, it's the only mutating WS event besides `join_meetup`/`leave_meetup`/`block_user` that skips it. `reason` (1716) and `client_content`/`message_content` (1717) are used verbatim with no length clamp before being embedded into `snapshot` (1758) and passed to `create_report`. In chat_db.py, the `reports` table (179-190) has `reason TEXT NOT NULL` and `message_snapshot TEXT NOT NULL` with no CHECK/length constraint, and `create_report` (1111-1136) inserts them unmodified. `purge_old_reports` (1158-1166) only deletes rows where `status IN ('actioned','dismissed') AND reviewed_at < cutoff` — a report sitting at the default `status='pending'` (set in the INSERT at line ~1121) is never touched by the purge loop, which runs this every 30s indefinitely (chat_ws.py:1839). Any authenticated user with access to a message (own room/DM/meetup membership is checked, but that's easy to satisfy — e.g. spam reports on messages in the main auto-join room) can call this repeatedly with large `reason`/`message_content` payloads (bounded only by the outer message-size check at `max_content` ~2000-4020 chars for the whole WS frame, still large) and the rows accumulate forever. Confirmed as described.

**Claim: C**
**Verdict: CONFIRMED**
**Reasoning:** WS `create_meetup` (chat_ws.py:1459-1461) guards with `if not manager.check_rate_limit(user_id): continue` before calling `create_meetup`. The REST endpoint `POST /chat/api/meetups` (chat_api.py:954-982) calls the identical `create_meetup` DB function (line 968) with no call to `manager.check_rate_limit` or any other cooldown/count/timestamp check — verified by reading the full endpoint body. It does require a valid session cookie via `_get_user_from_cookie` (401 if missing/invalid), so it's not anonymous, but any authenticated user can hit this REST path directly (not just through the chat.html WS client) and create meetups without any of the anti-spam protection the WS path enforces. Confirmed as described.
