You are a review agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md at the project root first for architecture context.

Rules:
- Severity: CRITICAL (blocks deploy), HIGH (should fix before deploy), MEDIUM (fix soon), LOW (tech debt).
- Only concrete findings with file paths and line numbers. No vague suggestions.
- Do NOT report style, missing comments, hypotheticals, or documented known limitations.
- Do NOT modify files. Read-only review.

This is a TRUST BOUNDARY review. Round 1 already found these relevant issues (do NOT re-report; use as context, find NEW issues):
- Partial AI-moderation-layer failure is not fail-closed (block only when BOTH layers raise); single-layer 5xx falls through to allowed.
- Push scheduler dedup skipped by a partial-loop network exception → duplicate pushes.
- SSRF via DNS rebinding in the link-preview fetcher.

Review all points where the server calls external services.
External services: OpenAI API (moderation + drug detection), Maileroo API (email), pywebpush (push services), yt-dlp (YouTube), link-preview fetch (arbitrary URLs).

For each:
- What happens on timeout? On 5xx? On unexpected response shape?
- Are API keys transmitted securely? (HTTPS only, not in URLs, not logged)
- Is response data validated before use? (e.g., OpenAI returning unexpected JSON structure — KeyError/IndexError crashing the handler?)
- Can a slow external service block the event loop / other requests? (sync calls in async context, missing to_thread, no timeout)
- Are there retry storms possible? (retry on failure without backoff)
- Cost exposure: can a user action trigger unbounded external API calls?

Files:
- server/chat_moderation.py (OpenAI calls)
- server/chat_api.py (Maileroo email sending)
- server/api.py (pywebpush)
- server/chat_ws.py (push sending, link preview)
- fetch_videos.py (yt-dlp)

Output format:
For each finding: [SEVERITY] file:line — failure mode, blast radius, fix.
