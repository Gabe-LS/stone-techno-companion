You are an adversarial verification agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md for architecture context. Be skeptical and try to REFUTE each claim. For each: (1) read the cited code plus surrounding context, (2) check for guards that prevent it, (3) check CLAUDE.md, (4) verify the precondition is reachable. Do NOT modify files — read-only.

=== CLAIM A (HIGH): deploy.sh health-check failure does not fail the script or trigger rollback ===
server/deploy.sh ~109-143: after Step 5's health/chat checks (~112-131), a non-"healthy" container status or failed chat API probe only prints a warning — the script has no exit 1 on that path and continues to backup cleanup and "Deploy complete" (~137-143), exiting 0. There is no automated rollback to the previous image/commit. Claim: a broken deploy (crash-looping container) goes unnoticed and cannot auto-recover. Verify the health-check branch does not exit non-zero and no rollback exists.

=== CLAIM B (HIGH): Scraper aborts entire run on a TBA/placeholder lineup slot (uncaught FK IntegrityError) ===
scraper/scrape.py ~187: in scrape_lineup's assignment loop, overlay_id = (li.get("data-overlay-identifier") or "").strip() has NO `if not overlay_id: continue` guard (unlike the overlay-detail loop ~144-146). Any lineup item matched by the assignments selector that lacks a data-overlay-identifier, or whose id has no matching artists entry, produces a schedule row referencing an artist_id with no artists row. INSERT OR IGNORE does NOT suppress FK violations in SQLite (upsert_lineup, scraper/db.py ~212-230), so this raises sqlite3.IntegrityError, uncaught, aborting the whole scrape/render run. Claim: a single TBA slot with no detail panel takes down regeneration. Verify the missing guard and that INSERT OR IGNORE does not suppress FK constraint violations.

=== OUTPUT (per claim) ===
Claim: A / B
Verdict: [CONFIRMED / REFUTED / DOWNGRADED to <severity>]
Reasoning: <explanation with specific file:line references.>
