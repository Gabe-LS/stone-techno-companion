You are an adversarial verification agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md for architecture context. Be skeptical and try to REFUTE the claim. For the claim: (1) read the cited code plus surrounding context, (2) check for guards/validations/architectural decisions that prevent it, (3) check CLAUDE.md for documented intentional trade-offs, (4) verify any required precondition is actually reachable. Do NOT modify files — read-only.

=== CLAIM (CRITICAL): Arbitrary file deletion via unvalidated media_url ===
In server/chat_ws.py, send_message validates an embedded media URL against _UPLOAD_URL_RE (^/chat/uploads/[a-f0-9]{32}\.(webp|mp4)$) around lines 1262-1276, but that check is skipped when is_e2ee_msg. Separately, around lines 1361-1370, the value actually persisted via create_message(..., media_url=...) is taken from the client's top-level `media_url` field (data.get("media_url")) and this block has NO validation guard at all (applies to E2EE AND non-E2EE messages). Later, delete_message (~lines 1683-1702) takes msg_row["media_url"] verbatim, does url.rsplit("/",1)[-1], and unlink()s it inside chat/uploads/. Claim: any authenticated user can send a message whose top-level media_url points at ANOTHER user's uploaded file (filename observable from any <img src> in any room), then delete their own message within the 120s window, causing the server to unlink the victim's file. Verify whether the stored media_url is truly unvalidated and whether delete_message truly unlinks it without ownership/path checks.

=== OUTPUT ===
Verdict: [CONFIRMED / REFUTED / DOWNGRADED to <severity>]
Reasoning: <explanation with specific file:line references. State clearly whether the non-E2EE top-level media_url path is also unvalidated.>
