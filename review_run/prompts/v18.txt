You are an adversarial verification agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md for architecture context. Be skeptical and try to REFUTE each claim. For each: (1) read the cited code plus surrounding context, (2) check for guards that prevent it, (3) check CLAUDE.md, (4) verify the precondition is reachable. Do NOT modify files — read-only.

=== CLAIM A (HIGH): Profile changes (name/username/avatar/color) are frozen inside already-open WebSocket connections ===
server/chat_ws.py handle_chat_ws reads display_name, username, color_index, avatar_url once at handshake into local closure variables (~1024-1030); every subsequent join_room presence broadcast (~590-621) and send_message broadcast (~862-873) reuses those frozen values. PUT /chat/api/profile (chat_api.py ~602-665) only writes to the users table — it never notifies chat_ws.manager or pushes an event to the live connection. The client WS reconnect handler never refetches /chat/api/me either. Claim: after a user renames/re-avatars, others watching a shared room keep seeing the old identity in real-time broadcasts and the member list until that user's socket actually disconnects/reconnects. Verify the identity fields are captured once at handshake and not refreshed on profile update.

=== CLAIM B (HIGH): Self-service account deletion never closes the user's live WebSocket connections ===
server/chat_api.py auth_delete_account (~494-516) deletes messages then calls delete_user (chat_db.py ~440-442, relying on ON DELETE CASCADE) but — unlike admin_delete_user (~2008-2038) and admin_ban (~1739-1787) which both explicitly ws.close(code=4003,...) for every connection in manager.user_conns[user_id] — it never touches manager.user_conns. Claim: any already-open socket for the deleted user stays live (keeps receiving broadcasts, can browse) until it attempts a write, which then raises an unhandled sqlite3.IntegrityError (FK violation against the cascade-deleted users row) caught by the generic handler, silently dropping just that one connection. Verify auth_delete_account does not close live connections while the admin paths do.

=== OUTPUT (per claim) ===
Claim: A / B
Verdict: [CONFIRMED / REFUTED / DOWNGRADED to <severity>]
Reasoning: <explanation with specific file:line references.>
