You are an adversarial verification agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md for architecture context. Be skeptical and try to REFUTE the claim. For the claim: (1) read the cited code plus surrounding context, (2) check for guards/validations that prevent it, (3) check CLAUDE.md, (4) verify the precondition is reachable. Do NOT modify files — read-only.

=== CLAIM (CRITICAL): Blocking Google OAuth network calls in async handlers + no rate limiting → event-loop stall DoS ===
auth_google (server/chat_api.py ~259-303) and auth_google_code (~305-367) make synchronous, blocking network calls directly inside async def route handlers with NO asyncio.to_thread wrapper and NO rate limiting:
- httpx.post("https://oauth2.googleapis.com/token", ...) at ~line 321 (called blocking, not awaited)
- google_id_token.verify_oauth2_token(...) at ~lines 275/339, which internally does a synchronous requests-based fetch of Google's public certs (cached after first success, but blocking until cached and again after cache expiry).
These routes are pre-authentication with zero rate limiting (no global middleware in server/api.py; no per-route _check_rate/_email_rate, unlike /login's limiter at chat_api.py ~371-376). Claim: an unauthenticated caller can flood /chat/api/auth/google/code with garbage `code` values; each request blocks the single asyncio event loop for the DNS+TLS+HTTP round trip to Google, serializing/stalling all other HTTP + WebSocket processing server-wide. Verify: (a) is httpx.post actually blocking (sync client, not awaited)?  (b) is verify_oauth2_token doing blocking IO in the event loop?  (c) is there truly no rate limit on these two routes?

=== OUTPUT ===
Verdict: [CONFIRMED / REFUTED / DOWNGRADED to <severity>]
Reasoning: <explanation with specific file:line references. Distinguish the blocking-call claim from the rate-limit claim; each may resolve differently.>
