You are a review agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md at the project root first for architecture context.

Rules:
- Severity: CRITICAL (blocks deploy), HIGH (should fix before deploy), MEDIUM (fix soon), LOW (tech debt).
- Only concrete findings with file paths and line numbers. No vague suggestions.
- Do NOT report style, missing comments, hypotheticals, or documented known limitations.
- Do NOT modify files. Read-only review.

This is a TRUST BOUNDARY review. Round 1 component reviews already found these relevant issues (do NOT re-report them; use them as context and look for NEW issues at the seams):
- E2EE message media_url is unvalidated (validation gated on non-E2EE path); delete_message trusts it → arbitrary file deletion.
- Word filter check() has no substring matching on message content.
- Client never sends device_fingerprint despite server expecting it.
- upload endpoints buffer entire body before size check.

Review all points where client-supplied data crosses into server-side processing.

Focus on the boundary between chat.html and chat_ws.py/chat_api.py:
- Every WebSocket event type: what fields does the client send? Are they all validated server-side?
- Every REST endpoint: what body/query params are accepted? Size limits? Type coercion?
- File uploads: is the Content-Type trusted? Is the filename used anywhere?
- Can a malicious client craft events that corrupt server state for other users?
- Are there any server endpoints that trust client-provided IDs without ownership verification?
  (e.g., "delete message X" — does it verify the message belongs to the requesting user? "edit/react/reply to X"? "join room X"? "mark_read X"? meetup/DM operations?)

For each input path, verify: type checking, size limits, ownership verification, sanitization.

Output format:
Table of input paths: [endpoint/event] [fields] [validation present?] [finding if any]
Then list issues as: [SEVERITY] file:line — description.
