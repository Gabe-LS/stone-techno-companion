You are an adversarial verification agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md for architecture context. Be skeptical and try to REFUTE each claim. For each: (1) read the cited code plus surrounding context, (2) check for guards that prevent it, (3) check CLAUDE.md, (4) verify the precondition is reachable. Do NOT modify files — read-only.

=== CLAIM A (HIGH): Word filter has no substring/embedded-word matching on message content ===
server/chat_moderation.py WordFilter.check (~116-132) tokenizes on whitespace and only matches when a normalized WHOLE word equals a blocklist term exactly (words[i:i+len(term_words)] == term_words). Gluing a flagged term into one token (e.g. "buymdmanow", "areyouanigger") never matches "mdma"/"nigger". Note the inconsistency: check_username (~143-147) HAS a substring fallback, but the message-content path check() has none. Claim: the instant pre-AI gate is trivially bypassed by removing spaces. Verify check() lacks the substring scan that check_username has.

=== CLAIM B (HIGH): Partial AI-moderation-layer failure is not fail-closed ===
server/chat_moderation.py moderate_message (~460-475): the block-on-failure branch only triggers when BOTH layers raise (`if ai_errored and drug_errored and os.environ.get("OPENAI_API_KEY")` ~469). check_openai_moderation (sexual/violence/hate/self-harm) and check_content_detection (drugs/spam/links) cover disjoint categories. If only the omni-moderation call 5xx's/times out while the drug layer succeeds and doesn't flag (because the content is e.g. violent/hateful, outside the drug layer's scope), the message falls through to return {"allowed": True} (~519). Claim: a single-layer AI outage silently approves messages that the failed layer would have caught. Verify the block condition requires BOTH to error and that a single-layer failure returns allowed.

=== OUTPUT (per claim) ===
Claim: A / B
Verdict: [CONFIRMED / REFUTED / DOWNGRADED to <severity>]
Reasoning: <explanation with specific file:line references.>
