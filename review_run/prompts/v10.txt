You are an adversarial verification agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md for architecture context. Be skeptical and try to REFUTE each claim. For each: (1) read the cited code plus surrounding context, (2) check for guards that prevent it, (3) check CLAUDE.md, (4) verify the precondition is reachable. Do NOT modify files — read-only.

=== CLAIM A (HIGH): Broadcast-storm — several room-fanout WS events have no rate limiting ===
In server/chat_ws.py, check_rate_limit (~1334, 1460) is only invoked for send_message and create_meetup. typing (~1444-1457), add_reaction/remove_reaction (~1601-1665), and join_room/leave_room presence (~580-643) all call broadcast/broadcast_to_room (O(room member count)) with zero throttling. Claim: a single authenticated connection spamming typing (or join/leave) fans out to every connected member of a large room (e.g. main event room) with no per-user cap — CPU/bandwidth amplification DoS. Verify these handlers lack a rate-limit call.

=== CLAIM B (HIGH): report_message has no rate limit / no size cap; pending reports never expire ===
server/chat_ws.py report_message (~1714-1770) never calls check_rate_limit; `reason` and `message_content` (stored verbatim in the snapshot) have no length truncation. In chat_db.py the reports table has no length CHECK, and purge_old_reports only deletes rows with status IN ('actioned','dismissed') — a status='pending' report is kept indefinitely. Claim: any user can flood the reports table with unbounded-size rows forever with no cleanup. Verify no rate limit, no size cap, and that pending reports are never purged.

=== CLAIM C (HIGH): meetup creation rate limit bypassable via REST ===
The WS create_meetup handler checks manager.check_rate_limit before creating (chat_ws.py ~1459-1461). The REST POST /chat/api/meetups (chat_api.py ~954-982) calls the identical create_meetup DB function with NO rate-limit check. Claim: a client using the REST path bypasses the anti-spam protection entirely. Verify the REST endpoint lacks the rate-limit check the WS path has.

=== OUTPUT (per claim) ===
Claim: A / B / C
Verdict: [CONFIRMED / REFUTED / DOWNGRADED to <severity>]
Reasoning: <explanation with specific file:line references.>
