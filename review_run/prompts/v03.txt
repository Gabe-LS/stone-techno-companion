You are an adversarial verification agent for the Stone Techno Companion project (FastAPI chat server + static site). Read CLAUDE.md for architecture context. Be skeptical and try to REFUTE the claim. For the claim: (1) read the cited code plus surrounding context, (2) check for guards/validations/architectural decisions that prevent it, (3) check CLAUDE.md for documented intentional trade-offs, (4) verify any required precondition is actually reachable. Do NOT modify files — read-only.

=== CLAIM (CRITICAL): E2EE DM silently downgrades to plaintext on transient key-fetch failure ===
In server/chat/chat.html, sendChatMessage (~lines 2493-2523) calls E2EE.encrypt, which calls _fetchDeviceList (~line 4314). _fetchDeviceList returns null only on a real HTTP 404; any other failure (network timeout, 500, dropped connection) is thrown as an Error (~line 4320) and propagates out of encrypt(). The catch block in sendChatMessage (~lines 2512-2522) treats a thrown exception identically to the legitimate "peer has no key" 404 case: it checks _roomEncrypted.has(roomId), and if false, falls through and sends the message as PLAINTEXT (outgoingContent never reassigned), showing only a dismissible toast. _roomEncrypted (declared ~line 405) is in-memory, reset each page load, populated only by a successful decrypt or a prior successful encrypt this session — NOT from the server's other_has_key flag. So a DM opened fresh this session (common given 24h DM TTL) plus one transient GET /chat/api/keys/{peer} failure => real previously-encrypted conversation sends plaintext to server. Claim: fail-open downgrade defeats E2EE guarantee. Verify the catch-block logic distinguishes (or fails to distinguish) 404 from transient errors, and whether _roomEncrypted is seeded from server state.

=== OUTPUT ===
Verdict: [CONFIRMED / REFUTED / DOWNGRADED to <severity>]
Reasoning: <explanation with specific file:line references.>
