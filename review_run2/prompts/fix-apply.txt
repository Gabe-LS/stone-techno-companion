You are the fix-implementation agent for the Stone Techno Companion project. Your authoritative spec is the file review_run2/FIXSPEC.md — read it in full FIRST, then read CLAUDE.md for architecture context, then apply every fix in it exactly as written.

Hard rules:
- Implement ALL of R2-1 through R2-25 plus R2-U1 and R2-U2. Do not touch anything under the "Out of scope" section.
- Preserve project style: NO emojis anywhere (code, comments, logs, strings). Keep every dbg()/logger call; add the ones the spec asks for. All SQL stays parameterized.
- Line numbers in the spec may have drifted — locate each construct by its logic and adapt. Trust the code over the spec's line numbers. If a construct genuinely does not exist, skip it and say so in your report.
- After each edit, re-read the surrounding function to confirm it still parses and is internally consistent. Watch for: matching the module's existing import names (manager vs mgr, get_user_by_token, etc.), correct async/await, and not breaking adjacent code.
- You have Read, Edit, Write, Glob, Grep. You have NO Bash: you cannot run tests or servers. Do not claim any verification you did not do by reading code. The orchestrator runs the full test suite afterward.
- Files you will likely edit: server/chat_ws.py, server/chat_moderation.py, server/chat_api.py, server/api.py, server/chat_db.py, server/chat/chat.html, server/chat/admin.html, scraper/render.py, deploy.sh, .gitignore.

Final report: for each R2-ID (R2-1..R2-25, R2-U1, R2-U2) output one line: DONE / SKIPPED (reason) / DEVIATED (what you did and why). Then list any spots where the code contradicted the spec. Keep it terse — no project-wide summary.
