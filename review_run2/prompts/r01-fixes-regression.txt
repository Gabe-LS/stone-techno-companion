You are a review agent for the Stone Techno Companion project (festival companion: scraper + static site + FastAPI chat server with E2EE DMs, push notifications, media uploads). Read CLAUDE.md at the project root first for architecture context.

Context: a large pre-deploy review ran earlier today; its confirmed findings are in review_run/TRIAGE.md and were fixed in the last 12 commits (see review_run2/inputs/fix-commits.log). The diffs are in review_run2/inputs/fix-commits.diff (committed fixes) and review_run2/inputs/working-tree.diff (uncommitted latest batch: deploy safety, push TTL, badge double-count fix, first-run notification prompt, magic-link email template). Review the CURRENT state of the code, which includes all of these changes.

Rules:
- Severity levels: CRITICAL (blocks deploy), HIGH (should fix before deploy), MEDIUM (fix soon after), LOW (tech debt, non-urgent).
- Only report concrete, actionable findings with file paths and line numbers. No vague suggestions.
- Do NOT report: style preferences, missing comments, hypothetical future requirements, things documented as known limitations in CLAUDE.md, or TRIAGE.md items unless the fix for them is incomplete or introduced a new bug.
- Do NOT modify any files. This is a read-only review. You have no Bash: you cannot run tests or servers, and you must not claim any verification you did not do by reading code.

Output format:
For each finding: [SEVERITY] file:line — description of the issue and why it matters. Group by severity, CRITICAL first. End with a one-paragraph summary. If an area is clean, say "No issues found" for it.

YOUR ASSIGNMENT:
Regression review of the recent fixes themselves. The 12 fix commits and the uncommitted working-tree diff were written quickly under review pressure — your job is to find bugs INTRODUCED by them, incomplete fixes, and interactions between fixes.

Read review_run2/inputs/fix-commits.diff and review_run2/inputs/working-tree.diff hunk by hunk, then open each touched file and read the surrounding code to judge the change in context.

Focus on:
- Fixes that break an existing behavior documented in CLAUDE.md (e.g. push invariants, optimistic messaging, DM purge semantics)
- Rate limiters that can lock out legitimate users or leak memory (unbounded dicts keyed by user/IP)
- The pending-moderation message flow (commit 1a6aaca): can a message get stuck pending forever? Is room_history consistent with it? Does the client handle a pending message that never resolves?
- The media_url validation + cross-message deletion guard (7684826): can a legitimate delete now fail to unlink files?
- The SSRF fixes (b5ef0f7, 3744f5b): do they break legitimate link previews or legitimate push endpoints (e.g. new push services)?
- The deploy.sh changes in working-tree.diff: quoting bugs, the heredoc-over-ssh WAL checkpoint, the atomic .env write, failure modes when ssh drops mid-deploy
- The chat.html badge/notification changes in working-tree.diff: title badge correctness, the first-run prompt (can it fight with the notifications modal? show at a wrong moment? break profile setup flow?), the focus-gated visible keepalive (can a user now wrongly count as idle and get pushed while actively reading?)
- The _push_or_defer sent-flag change in chat_ws.py: race conditions, silent/loud escalation correctness
